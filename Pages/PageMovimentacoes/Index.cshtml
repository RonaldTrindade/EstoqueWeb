@page
@model EstoqueWeb.Pages.PageMovimentacoes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SaidaEntradaProduto[0].TipoOperacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaidaEntradaProduto[0].Quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaidaEntradaProduto[0].DataOperacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaidaEntradaProduto[0].Produto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaidaEntradaProduto[0].Estoque)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SaidaEntradaProduto) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoOperacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataOperacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Produto.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estoque.Localizacao)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
